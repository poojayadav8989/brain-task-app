version: 0.2

phases:
  install:
    commands:
      - echo "Install phase working"

  pre_build:
    commands:
      - echo "Pre-build phase working"

  build:
    commands:
      - echo "Build phase working"
      - echo "Hello, CodeBuild! This is a test build."

  post_build:
    commands:
      - echo "Post-build phase working"
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
